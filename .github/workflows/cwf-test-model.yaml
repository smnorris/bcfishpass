name: cwf-test-model
run-name: ${{ github.actor }} Run bcfishpass on CWF test db
on:
  workflow_dispatch:
  pull_request:
    branches:
      - 'main'
    paths:
      - 'model/**'
concurrency:
  group: cwf
env:
  DATABASE_URL: ${{ secrets.DATABASE_URL }}
  DATABASE_HOST: ${{ secrets.DATABASE_HOST }}
  SSH_USER: ${{ secrets.SSH_USER }}
  SSH_KEY: ${{ secrets.SSH_KEY }}
  SSH_HOST: ${{ secrets.SSH_HOST }}
jobs:
  load-csv:
    runs-on: ubuntu-latest
    environment: cwf-test
    container: ghcr.io/smnorris/bcfishpass-runner:main
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Load weekly refresh
        run:  |
          cp parameters/example_cwf/*csv parameters/
          jobs/connect && jobs/load_csv
          psql -v ON_ERROR_STOP=1 $DATABASE_URL -c "DELETE FROM bcfishpass.parameters_habitat_method WHERE watershed_group_code NOT IN (SELECT watershed_group_code FROM bcfishpass.wcrp_watersheds)"
          cd jobs && ./load_weekly
  load-xings-observations:
    runs-on: ubuntu-latest
    environment: cwf-test
    container: ghcr.io/smnorris/bcfishpass-runner:main
    needs: load-csv
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Load pre-processed data
        run:  |
          jobs/connect
          jobs/load_modelled_stream_crossings
          jobs/load_observations
  model-access-natural:
    runs-on: ubuntu-latest
    environment: cwf-test
    container: ghcr.io/smnorris/bcfishpass-runner:main
    needs: load-xings-observations
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Access model - natural
        run:  |
          jobs/connect
          cd model/01_access && ./01_model_access_natural.sh bt ch_cm_co_pk_sk st wct
  model-access-anthropogenic:
    runs-on: ubuntu-latest
    environment: cwf-test
    container: ghcr.io/smnorris/bcfishpass-runner:main
    needs: model-access-natural
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Access model - anthropogenic
        run:  |
          jobs/connect
          cd model/01_access && ./02_model_access_anthropogenic.sh
  model-habitat-linear:
    runs-on: ubuntu-latest
    environment: cwf-test
    container: ghcr.io/smnorris/bcfishpass-runner:main
    needs: model-access-anthropogenic
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
        with:
            fetch-tags: true
            fetch-depth: 0
      - name: Linear habitat model
        run:  |
          git config --global --add safe.directory /__w/${{ github.event.repository.name }}/${{ github.event.repository.name }}
          jobs/connect
          cd model/02_habitat_linear
          ./habitat_linear.sh
      - name: Rank crossings
        run:  |
          psql $DATABASE_URL -v ON_ERROR_STOP=1 -f wcrp/scripts/sql/barriers_sidechannel.sql
          psql $DATABASE_URL -v ON_ERROR_STOP=1 -f wcrp/scripts/sql/load_crossings_upstream_habitat_wcrp.sql
          psql $DATABASE_URL -v ON_ERROR_STOP=1 -f wcrp/scripts/sql/load_crossings_wcrp.sql
          python wcrp/scripts/auto_rank.py
          python wcrp/scripts/habitat_connectivity.py load
  wcrp-reporting:
    runs-on: ubuntu-latest
    environment: cwf-test
    container: ghcr.io/smnorris/bcfishpass-runner:main
    needs: model-habitat-linear
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Rank crossings
        run:  |
          jobs/connect
          psql $DATABASE_URL -v ON_ERROR_STOP=1 -f wcrp/scripts/sql/barriers_sidechannel.sql
          psql $DATABASE_URL -v ON_ERROR_STOP=1 -f wcrp/scripts/sql/load_crossings_upstream_habitat_wcrp.sql
          psql $DATABASE_URL -v ON_ERROR_STOP=1 -f wcrp/scripts/sql/load_crossings_wcrp.sql
          python wcrp/scripts/auto_rank.py
          python wcrp/scripts/habitat_connectivity.py load        